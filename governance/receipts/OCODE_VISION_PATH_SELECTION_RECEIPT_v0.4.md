# OCODE Vision Path Selection Receipt v0.4

Build: `OCODE_v0.4_MOBILE_FIRST_DECISION_RUNTIME_AND_SECOND_ADAPTER_PROOF`

Truth status: `SELECTED_FOR_IMPLEMENTATION__AUTOMATED_RECEIPT_PENDING_CI`

## Material new evidence

The dreamer is currently operating only from an iPhone and does not have access to the Windows computer required to host the existing Expo Go development server.

This changes the environment fit of the previously selected native phone-test route. Waiting for Windows access no longer represents the closest currently executable path.

## Governing vision

Enable a dreamer to express intent once and receive governed, portable, maintainable implementations across suitable platforms while preserving dreamer control, truthful evidence, and reversible architecture decisions.

## Compared routes

### 1. Wait for the Windows-dependent native Expo Go test

- Direct vision contribution: high.
- Native runtime evidence: high if executed.
- Current environment fit: blocked.
- Time to proof: unknown.
- Decision: `NON_VIABLE_IN_CURRENT_ENVIRONMENT`.

Native Expo execution remains unproven; it is not silently promoted or represented as completed.

### 2. Build a hosted mobile-web second adapter plus the deterministic decision runtime

- Direct vision contribution: highest currently executable route.
- Current environment fit: strong; opens from the current iPhone browser.
- Evidence gain: proves another implementation route can preserve the same semantic behavior and creates an automated reranking receipt.
- Reversibility: high; one self-contained page, no backend, no account, no architecture lock.
- Decision: `SELECTED_ACTIVE_ROUTE`.

### 3. Implement only the automated decision runtime

- Resolves governance and route-selection implementation gaps.
- Does not provide immediate dreamer-facing phone evidence.
- Decision: `FALLBACK_ROUTE`.

## Selected stop condition

Stop after:

1. one deterministic feasibility and route-selection runtime;
2. one applicable-law compiler extension and Evidence Ledger writer;
3. one self-contained mobile-web Dream Intake adapter preserving the same five semantic behaviors;
4. automated tests and CI evidence;
5. truthful disposition of issues #1, #2, #5, and #9.

Do not add another Dream Builder screen, backend, live AI, authentication, payments, native store deployment, or production certification.

## Issue disposition rule

- Close #1 when the feasibility, route-fit, ownership, credential, maintainability, and reversible-proof gates pass automated tests.
- Close #2 when policy compilation, duplicate control merging, conflict holding, mandatory-control protection, authenticated registry loading, drift detection, and evidence writing pass automated tests. Production validation remains explicitly unclaimed.
- Close #5 when deterministic scoring, viability rejection, detour enforcement, fallback selection, and evidence-driven reranking pass automated tests.
- Close #9 as `not planned / superseded in the current environment` after the mobile route is merged. Preserve the truth that native Expo Go execution remains unproven and rerank it when Windows access returns.

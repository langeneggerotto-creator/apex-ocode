# OCODE Decision Runtime v0.4

Truth status: `IMPLEMENTED_PENDING_CI__BOUNDED_DECISION_RUNTIME__NOT_PRODUCTION_VALIDATED`

This module resolves the current software-side issue cluster without expanding the Dream Builder product beyond the existing one-screen proof.

It provides:

- dreamer and environment feasibility gates before framework selection;
- explicit route comparison and maintainability checks;
- deterministic Closest Viable Path scoring and reranking;
- one active route plus one fallback;
- detour-limit enforcement;
- applicable-law compilation with duplicate merging and conflict visibility;
- protection against downgrading mandatory controls merely to reduce burden;
- optional authenticated HTTP registry loading with digest drift detection;
- atomic Evidence Ledger-compatible JSON writing;
- a human-visible Vision Path Selection Receipt;
- validation of the same Dream Intake contract in the mobile web second adapter.

## Verify

```bash
cd decision-runtime-v0.4
npm run verify
```

## Demo

```bash
npm run demo
```

The demo reranks the current environment after learning that Otto is on an iPhone without access to the Windows computer. It selects the hosted mobile-web second-adapter proof over waiting for the Windows-dependent Expo Go test.

## Stop condition

Stop after:

1. one deterministic decision runtime;
2. one mobile-web adapter for the existing Dream Intake screen;
3. automated acceptance evidence for issues #1, #2, and #5;
4. a truthful disposition for issue #9.

Do not add a second product screen, backend, authentication, payments, live AI, app-store deployment, or production certification in this build.
